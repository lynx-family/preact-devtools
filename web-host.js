// Subpath entry: `import "@lynx-js/preact-devtools/web-host"` (the package has
// no exports map, so this root file makes the bare subpath resolvable).
module.exports = require("./lib/react-lynx/web-host.js");
